# 73-cpp.34.25.18 手機漸進式渲染

- 角色卡核心資料先顯示，職業／血脈／大道／技能模板等次要資料於背景補齊。
- 遊戲間同樣先顯示角色核心，再載商店／書庫／命隙附加資料。
- `/api/character` 暫時失敗時保留上一份成功資料，不再把角色卡清空。
- 元素自生速率改成低優先 idle 更新，不阻塞角色卡。
- 手機將同一瞬間多個 render 合併約 24ms；頁面在背景時不重畫，回前景再補一次。
