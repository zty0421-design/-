# 73-cpp.34.25.13 — 全站畫面按需載入與容錯

本版把登入後的全量背景讀取改為按畫面載入。DM 登入只先取得角色、使用者、房間等核心資料；其他管理模組在切換到該頁時才讀取。玩家頁面也統一採先顯示 UI、再背景取得資料。

每個玩家／DM 畫面都有獨立 loading、timeout、error 與 retry 狀態。單一 API 或單一 HTML renderer 出錯，不再讓其他頁面一起空白。舊 localStorage 保存已移除的 DM 頁面時會自動回到有效控制台。
