# 73-cpp.34.25.20

- 新增角色 `personality_profile` JSONB，作為「特殊設定・人格」的可擴充資料。
- DM 可設定人格名稱、標籤、描述與玩家可見性。
- 玩家角色卡只在允許時顯示人格；DM 永遠可查看。
- 元素別名統一：陽=光、陰=暗。後端戰鬥與元素儲存會正規化到「光／暗」。
- 前端顯示為「光（陽）／暗（陰）」但保存標準 key。
- 渲染優化：大量圖片的 lazy/async 屬性改分幀處理，避免大型角色/NPC列表一次掃描阻塞主執行緒。
- 載入優化：本次沿用 view lazy loader / GET in-flight dedupe，不新增登入首屏依賴。
