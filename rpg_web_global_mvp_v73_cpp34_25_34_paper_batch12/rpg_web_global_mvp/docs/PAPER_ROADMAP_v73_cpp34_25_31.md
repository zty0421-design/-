# 73-cpp.34.25.31｜紙本清單第九批

## 世界地圖視覺化
- 使用既有地圖節點座標、類型與連線直接生成幻想式世界地圖。
- 不使用衛星圖，不新增外部地圖服務。
- 目前位置與相鄰道路醒目顯示。
- 玩家不顯示尚未揭露的隱藏節點；DM 仍可看見。
- 0 額外 API：重用 `state.mapNodes`。

## 命隙美術深化
- 加入低成本星塵層與立繪光暈。
- 彌夜頭像／立繪改用 lazy loading + async decoding。
- reduced-motion 模式停用星塵動畫。

## 效能
- 世界圖只使用 SVG 線段 + 輕量 HTML 地標，不載入地圖 SDK。
- 不新增輪詢、不新增世界頁 API。
